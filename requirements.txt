# Python dependencies for the video editing agent
# Install: pip install -r requirements.txt
#
# Not installed via pip (set up separately):
# - FFmpeg + FFprobe on your PATH (Whisper and trim/merge need the real binaries)
# - Ollama app running locally; pull model: ollama pull llama3:latest

requests>=2.28.0
openai-whisper
